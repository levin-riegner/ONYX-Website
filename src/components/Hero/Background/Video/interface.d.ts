// Imports
// ------------

// Exports
// ------------
export type VideoProps = {
	videoSrc: string;
	/** WebVTT URL for captions (defaults to ambient / no-dialogue track for decorative backgrounds) */
	captionsSrc?: string;
};
